#include <stdio.h>
#include <stdlib.h>

int main()
{
    int max;
    int x , y;

    for(int i=2;i<x;i++){
            int x , y;
       printf("please enter valus x and y:\n");
       scanf("%d%d", &x, &y);
       if(y%x!=0){
        y % x = max;
       }

       printf("cbf",max);

    }
    return 0;
}
